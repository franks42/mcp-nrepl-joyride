#!/bin/bash

# Convenient wrapper for nrepl_test_server.py
# Usage: ./nrepl-test [start|stop|restart|status]

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

python3 "$SCRIPT_DIR/nrepl_test_server.py" "$@"