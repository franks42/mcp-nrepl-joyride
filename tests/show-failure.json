{
  "show_error": [
    {
      "name": "connect_first",
      "tool": "nrepl-server",
      "args": {"op": "connect", "connection": "56626"},
      "description": "Make sure connected"
    },
    {
      "name": "failing_test",
      "tool": "debug-eval",
      "args": {"code": "(let [s (:socket @nrepl-mcp-server.state.connection/connection-state)] (when s {:local-port (.getLocalPort s) :remote-port (.getPort s)}))"},
      "description": "What exactly fails here?"
    }
  ]
}